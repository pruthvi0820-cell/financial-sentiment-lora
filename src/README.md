# Source Code

This folder is reserved for reusable Python source code extracted from the experimentation notebook.

The current implementation is contained in:

`notebooks/financial_sentiment_lora.ipynb`

The project may later separate preprocessing, model training, evaluation, and inference into dedicated Python modules.
